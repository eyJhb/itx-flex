<UserControl x:Class="Itx.Flex.Client.View.ExamTimeLeftView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:Itx.Flex.Client.ViewModel"
             xmlns:view="clr-namespace:Itx.Flex.Client.View">
  <Grid>
    <ProgressBar Height="20" Value="{Binding ProgressBarCurrent, Mode=OneWay}"
                 Minimum="{Binding ProgressBarMinimum}" Maximum="{Binding ProgressBarMaximum}"/>
    <TextBlock FontWeight="Bold" HorizontalAlignment="Center" Text="{Binding OngoingExamTimeLeftText}"/>
  </Grid>
</UserControl>